{
  "name": "@tailux/docs",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "@tailux/tsconfig": "workspace:*"
  }
}
