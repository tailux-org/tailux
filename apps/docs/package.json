{
  "name": "@tailux/docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "@tailux/tsconfig": "workspace:*"
  }
}